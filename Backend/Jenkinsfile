pipeline {
    agent any
    stages {
        stage ("Clone Git") {
            steps {
                git branch: 'main', url: 'https://github.com/nisharathod231/Kalopsia.git/Backend'
            }
        }
        stage ("Build via Maven") {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}
